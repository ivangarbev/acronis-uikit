{
  "name": "@acronis-platform/icons-svg",
  "version": "0.1.0",
  "private": true,
  "description": "Raw SVG icon sources for Acronis — monocolor and multicolor icons fetched from Figma, plus per-page JSON manifests.",
  "type": "module",
  "exports": {
    "./package.json": "./package.json",
    "./monocolor/*": "./src/monocolor-icons/*.svg",
    "./multicolor/*": "./src/multicolor-icons/*.svg",
    "./svg/*": "./src/svg/*.svg",
    "./figma/*": "./src/figma/*.json"
  },
  "scripts": {
    "build": "echo \"no build (raw SVG source package)\" && exit 0",
    "dev": "echo \"no dev server (raw SVG source package)\" && exit 0",
    "clean": "echo \"nothing to clean (raw SVG source package)\" && exit 0",
    "pull-icons": "tsx ../../tools/figma-icons-fetcher/src/index.ts",
    "fix-viewbox": "tsx scripts/fix-negative-viewbox.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "echo \"no tests (raw SVG source package)\" && exit 0",
    "test:watch": "echo \"no watch mode (raw SVG source package)\" && exit 0",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [
    "acronis",
    "icons",
    "svg",
    "design-system",
    "monocolor",
    "multicolor"
  ],
  "devDependencies": {
    "@acronis-platform/figma-icons-fetcher": "workspace:*",
    "@types/node": "catalog:",
    "tsx": "^4.20.0",
    "typescript": "catalog:"
  }
}
